# ========================================
# LEARNEASE SERVER CONFIGURATION
# ========================================
# 
# This is a TEMPLATE file showing what environment variables are needed.
# 
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual credentials in .env
# 3. NEVER commit .env to Git (it's in .gitignore)
# 4. Only .env.example should be in Git
#
# ========================================

# EMAIL CONFIGURATION
# =================
# Get Gmail App Password from: https://myaccount.google.com/apppasswords
# Note: Use App Password, NOT your regular Gmail password
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_gmail_app_password_16_chars
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# MONGODB CONFIGURATION
# ====================
# Option 1: MongoDB Atlas (Cloud - Recommended)
#   1. Go to https://www.mongodb.com/cloud/atlas
#   2. Create free cluster
#   3. Get connection string
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/learnease

# Option 2: Local MongoDB
# MONGO_URI=mongodb://localhost:27017/learnease

# JWT CONFIGURATION
# ================
# Generate strong secret: openssl rand -base64 32
# Used for signing auth tokens
JWT_SECRET=your_strong_jwt_secret_at_least_32_characters_long

# GOOGLE API CONFIGURATION
# =======================
# For AI/ML features (if needed)
# Get from: https://console.cloud.google.com/apis
GOOGLE_API_KEY=your_google_cloud_api_key

# SERVER CONFIGURATION
# ===================
PORT=8080
SERVER_HOST=localhost

# Optional: For production, use:
# SERVER_HOST=0.0.0.0
# And set NODE_ENV=production
NODE_ENV=development

# ========================================
# DO NOT SHARE THIS FILE WITH .env FILLED
# ========================================
